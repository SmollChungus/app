import {faMagnet} from '@fortawesome/free-solid-svg-icons';
import {FontAwesomeIcon} from '@fortawesome/react-fontawesome';

export const title = 'Analog';
export const component = (props: any) => <FontAwesomeIcon icon={faMagnet} />;
